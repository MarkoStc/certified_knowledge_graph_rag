# Virtual environments
.venv/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Build artifacts
dist/
build/

# Secrets (AGENTS.md §3.1: HF_TOKEN etc. live in .env, never in git)
.env
.env.*

# Large artifacts — live on $SCRATCH, never in the repo (AGENTS.md §2)
data/
models/
logs/
wandb/
runs/

# Baseline repos cloned for comparison (AGENTS.md §3.6)
third_party/

# Editor and OS noise
.idea/
.vscode/
.DS_Store

# NOTE: uv.lock is intentionally tracked — do not ignore it.
